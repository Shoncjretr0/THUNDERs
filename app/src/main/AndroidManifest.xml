<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thunder">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/cloud"
        android:label="Thunder"
        android:roundIcon="@drawable/cloud"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".messagecontact"></activity>
        <activity android:name=".messagedocument" />
        <activity android:name=".audioshow" />
        <activity android:name=".messageaudio" />
        <activity android:name=".searchhashtag" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="enter the gmap key" />

        <activity
            android:name=".test"
            android:label="title_activity_test" />
        <activity android:name=".messagelocation" />
        <activity android:name=".messagephotopass" />
        <activity android:name=".chatlayout" />
        <activity android:name=".userchatprof" />
        <activity android:name=".messaging" />
        <activity android:name=".chatmenu" />
        <activity android:name=".friends_list" />
        <activity android:name=".profilevedio_list" />
        <activity android:name=".profilephotolist" />
        <activity android:name=".profile_vedios" />
        <activity android:name=".profile_photos" />
        <activity android:name=".video_list" />
        <activity android:name=".profile" />
        <activity android:name=".notification" />
        <activity android:name=".postnavigation" />
        <activity android:name=".search" />
        <activity android:name=".videos" />
        <activity android:name=".photos" />
        <activity android:name=".userprofile" />
        <activity android:name=".forgotpassword" />
        <activity
            android:name=".register"
            android:label="Register" />
        <activity
            android:name=".login"
            android:label="login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
